<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="320dp"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="@android:color/white">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="12dp">

        <TextView
            android:id="@+id/tv_captcha_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="安全验证"
            android:textSize="16sp"
            android:textColor="#333333"
            android:textStyle="bold"
            android:layout_centerVertical="true" />

        <ImageView
            android:id="@+id/iv_captcha_close"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:src="@drawable/close"
            android:contentDescription="关闭"
            android:padding="2dp"
            android:background="?attr/selectableItemBackgroundBorderless" />
    </RelativeLayout>

    <!-- 验证码图片区域 (CSF对齐: 2:1 aspect ratio → 320dp:160dp) -->
    <FrameLayout
        android:id="@+id/fl_captcha_container"
        android:layout_width="match_parent"
        android:layout_height="160dp"
        android:background="#E8E8E8">

        <!-- 背景图(带阴影的完整拼图) -->
        <ImageView
            android:id="@+id/iv_shade_image"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="fitXY"
            android:contentDescription="验证码背景" />

        <!-- 滑块小块图(跟SeekBar移动) -->
        <ImageView
            android:id="@+id/iv_cutout_image"
            android:layout_width="56dp"
            android:layout_height="match_parent"
            android:scaleType="fitXY"
            android:contentDescription="滑块" />
    </FrameLayout>

    <!-- 提示文字 -->
    <TextView
        android:id="@+id/tv_slider_hint"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:text="请滑动滑块完成验证"
        android:textSize="13sp"
        android:textColor="#999999"
        android:padding="8dp" />

    <!-- 系统SeekBar (CSF对齐: Material3 Slider → Android SeekBar) -->
    <SeekBar
        android:id="@+id/seekbar_captcha"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:max="1000"
        android:progress="0"
        android:paddingStart="0dp"
        android:paddingEnd="0dp" />

    <Button
        android:id="@+id/btn_captcha_confirm"
        android:layout_width="match_parent"
        android:layout_height="44dp"
        android:layout_marginTop="8dp"
        android:background="@drawable/app_comm_radius_bg"
        android:text="确认"
        android:textColor="@android:color/white"
        android:enabled="false" />

</LinearLayout>
